# local tooling
.claude/

# build
node_modules/
# The built single file is committed so the game stays one download away.
# Everything else vite emits is not.
dist/*
!dist/index.html
# The server bundle is built by the image, not committed.
dist-server/

# OS artifacts
Thumbs.db
.DS_Store
