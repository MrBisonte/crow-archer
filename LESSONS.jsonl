{"id":"worktree-git-file-redirect","date":"2026-08-30","topic":"worktree","level":"trap","lesson":"A git worktree lives inside the main repo at .claude/worktrees/<name>. If its .git file goes missing (pruned or deleted while a session is still running) every git command from that directory silently resolves to the main clone and reports success while driving the wrong repo. Run `git rev-parse --show-toplevel` before any state-changing git command, and prefer `git -C <path>` over relying on the shell's working directory. Caught three sessions, Merger's own included.","refs":["CLAUDE.md"],"session":"multiple"}
{"id":"two-local-clones","date":"2026-08-30","topic":"clones","level":"trap","lesson":"Two independent clones of this repo sit on disk, labs/robinhood and labs/crow-archer, with separate refs and separately-stale masters. A branch you cannot find is probably in the other clone. Fetch and fast-forward the specific clone you mean before cutting a branch from its master.","refs":[]}
{"id":"clean-merge-drops-prose","date":"2026-08-30","topic":"merge","level":"trap","lesson":"A clean auto-merge with a fully green suite can still drop content. In round 3 it lost 154 lines of docs/architecture.md and docs/manual.md with no conflict, because no test reads prose. After any merge, diff the result against each source branch and read what was not taken, do not trust green plus no-conflict.","refs":[]}
{"id":"git-ancestry-lies","date":"2026-08-30","topic":"git-ancestry","level":"trap","lesson":"Ancestry lies here three ways: squash merges, dropped content whose merge-base became the source tip, and cherry-picks that change the hash. So `N commits ahead` is never proof a branch is unmerged. Verify by content: `git diff --stat origin/master <branch>` read for ADDED lines, or compare `git rev-parse <branch>^{tree}` against master's tree hash.","refs":["worktree-git-file-redirect"]}
{"id":"branch-d-refuses-squash","date":"2026-08-30","topic":"branch-cleanup","level":"trap","lesson":"`git branch -d` refuses a squash-merged branch as `not fully merged` even when every line of it is already on master. That refusal is not evidence of unmerged work. Confirm the branch is contained by content first, then delete with `-D`.","refs":["git-ancestry-lies"]}
{"id":"duplicate-fix-across-sessions","date":"2026-08-30","topic":"coordination","level":"trap","lesson":"Two sessions can fix the same thing two different ways. Round 4 had it twice: the archer-pierce flake, and a balance-doc drift test written once standalone and once inline. Read the ledger before starting; if your task is already someone's row, say so instead of duplicating the work.","refs":[]}
{"id":"hidden-pane-raf-starvation","date":"2026-08-30","topic":"harness","level":"trap","lesson":"The in-app Browser pane starves requestAnimationFrame while document.visibilityState still reads 'visible', freezing the game clock from page load. This is what the abandoned `freeze at iteration 51` synthetic repro was actually measuring, not a game bug. Drive the loop with `setInterval(() => __game.frame(16.7), 16)` (setInterval still fires, and frame() keeps liveLoop true unlike takeClock()), or verify in a real browser tab.","refs":["src/dev/flight-recorder.ts","monitored-playtest-beats"],"session":"flight-recorder"}
{"id":"routeless-agent-invalidate","date":"2026-08-30","topic":"pathfinding","level":"fix","lesson":"The crow king's bats ride in the crows array with no `path` field until the scheduler serves them. A tile turning solid in that 1-2 frame window ran invalidateThrough over the whole roster and read `.length` off undefined, throwing inside the rAF callback and freezing the game mid boss fight. Fix: treat a missing path as routeless (`== null`, not `=== null`) and mark `path` optional on PathAgent. A roster that mixes walkers with never-pathing agents must not let one field-less member take the frame loop down.","refs":["src/sim/pathfinding.ts","hidden-pane-raf-starvation"],"session":"fix/invalidate-routeless-agents"}
{"id":"wasd-shift-latch","date":"2026-08-30","topic":"input","level":"fix","lesson":"keyDownAs[e.code] held one name per physical key. Holding a key across a Shift press makes the OS auto-repeat it under its shifted name (w becomes W), orphaning the first entry so keyup clears the wrong one and the key jams its own movement axis. Arrow keys are immune because their name does not shift. Fix: keep the down/up bookkeeping in pure noteKeyDown/noteKeyUp in src/sim/input.ts and release a code's old name when it repeats under a new one.","refs":["src/sim/input.ts"],"session":"fix/wasd-shift-latch"}
{"id":"monitored-playtest-beats","date":"2026-08-30","topic":"diagnostics","level":"practice","lesson":"An intermittent freeze that no synthetic step-loop could reproduce fell in one monitored play session. The instrument: a one-second heartbeat POSTing game vitals plus the drained diagnostic log to a dev-server JSONL sink, with watchdogs that classify the stop (loop-dead, logic-freeze, no-frames, or a hard hang detected by the beat gap from outside). Reach for real monitored play with an out-of-page log sink before trusting a headless repro of a timing bug.","refs":["docs/playbooks/monitored-playtest.md","src/dev/flight-recorder.ts"],"session":"flight-recorder"}
{"id": "heredoc-backslash-collapse", "date": "2026-09-03", "topic": "shell", "level": "trap", "lesson": "A doubled backslash inside a quoted heredoc collapses before Python sees it, so a match pattern written as \\\\n arrives as a REAL newline and a multi-line str.replace() pattern built to match source text containing \\n scores zero hits. Cost two failed edits in one session, including one spent adding CRLF handling on the theory that line endings were the cause -- the file was LF and the pattern itself was wrong. Anchor a scripted edit on a backslash-free substring, or build the character with chr(92); diagnose a mystery zero-match by printing repr() of each pattern line against the file's real text before blaming line endings. The assert-the-match-count rule is the only reason this failed loudly instead of shipping a no-op edit.", "refs": ["CLAUDE.md"], "session": "feat/talent-icons"}
{"id": "merged-table-duplicate-key", "date": "2026-09-03", "topic": "merge", "level": "trap", "lesson": "Two branches each added a row to the same JS object literal (the talent STATS table) under the same key. The merge was CLEAN -- the hunks did not overlap -- and produced an object with two longThrow keys, where the language silently keeps the last one and the archer's meaning of the stat won over the wizard's. No test caught it because both keys are valid syntax and the type is an index signature. Reading a merged TABLE row by row is a separate step from reading a merged DIFF: after any merge that touches a shared table, list its keys and assert they are unique.", "refs": ["src/sim/talents.ts", "clean-merge-drops-prose"], "session": "reconcile/talents-on-r7"}
{"id": "mixed-line-endings-in-one-file", "date": "2026-09-03", "topic": "shell", "level": "trap", "lesson": "game.test.ts is CRLF, but a block appended with a Bash heredoc arrives LF, so one file ends up with both. A scripted edit that joins its pattern with CRLF then matches nothing in the LF region and vice versa, and the assert-the-match-count rule fires on a pattern that is character-for-character correct. Cost three failed edits before the cause was visible. Fix: have the edit try both endings (replace chr(10) with CRLF in the pattern and retry), or work per line with splitlines(keepends=True) so each line keeps its own ending. Diagnose by printing repr() of the region rather than reading it, since both endings look identical in any viewer.", "refs": ["heredoc-backslash-collapse", "src/legacy/game.test.ts"], "session": "feat/ultimates"}
{"id": "green-alone-red-in-suite", "date": "2026-09-03", "topic": "tests", "level": "trap", "lesson": "Four new tests passed with -t and failed in the full run, from two causes that both look like flakiness and are neither. First: assigning player.aimAngle does not aim. updatePlayer copies it from the pointer on every step, so the aim is whatever the PREVIOUS test left the mouse at -- aim with the arena-testkit aimAt helper and step a frame before firing. Second: crow speed and aggression climb with the run's escalation clock, which earlier tests have already advanced, so a crow that sits still in isolation outruns the thing being measured in a suite. Park a test's target by setting heldTimer, the same hold the ranger's net applies, or assert on terrain, which does not move or get replaced. A count of enemies is also the wrong assertion while waves are spawning: compare by identity.", "refs": ["src/legacy/arena-testkit.ts", "src/legacy/game.test.ts"], "session": "feat/ultimates"}
{"id": "enforce-the-lesson-not-just-record-it", "date": "2026-09-03", "topic": "coordination", "level": "practice", "lesson": "A pass over the fourteen lessons in this file asked of each one: is there anything that would catch it, or does it rely on someone having read this? Three were mechanisable and none of the three had a guard. merged-table-duplicate-key is now caught by a uniqueness check over every talent and rite id in src/sim/talents.test.ts -- the language refuses two keys in one object literal but has nothing to say about two ARRAY entries sharing an id, which is why the clean merge went through. mixed-line-endings-in-one-file is now caught by src/line-endings.test.ts, which rejects a file at war with itself in either direction rather than mandating an ending. Half of green-alone-red-in-suite is pinned as a characterisation test in game.test.ts: assigning player.aimAngle does not aim, because updatePlayer copies the angle from the pointer every frame. The other eleven are about git, worktrees, clones and the harness, where the cost is real and a test is not the instrument. Worth repeating when this file grows: a lesson nobody has turned into a guard is a lesson the next session pays for again.", "refs": ["merged-table-duplicate-key", "mixed-line-endings-in-one-file", "green-alone-red-in-suite", "src/line-endings.test.ts", "src/sim/talents.test.ts"], "session": "feat/ultimates"}
{"id": "new-hit-shape-forgets-the-garrison", "date": "2026-09-05", "topic": "combat", "level": "trap", "lesson": "Third time this repo has shipped code that quietly skips soldiers. The arrow-vs-body loops handle crows, skeletons and soldiers in three copies, and only the first two routed through spendArrowPierce -- the soldier loop spliced the arrow itself. So everything hanging off that shared call silently did not happen against the garrison: an ultimate sold as piercing without limit stopped dead on the first soldier, and a harpoon meant to reel the hero in never reeled off one. damageEnemiesInRadius carries a comment about the exact same omission from an earlier release. The fix that stops a fourth: one walk over the three lists (forEachHostile), so a new hit SHAPE cannot enumerate two of them and forget the third. When adding anything that hits bodies, grep for the three lists together and make sure you are not writing a fourth enumeration.", "refs": ["src/legacy/game.js", "routeless-agent-invalidate"], "session": "feat/ultimates"}
{"id": "tests-that-race-a-state-machine", "date": "2026-09-05", "topic": "tests", "level": "trap", "lesson": "A test fired a projectile 300 px at a boss and asserted the effect of the hit. It failed two runs in five, and pinning the boss's position and shield every frame did not fix it -- the shield comes back mid-flight, the bolt is absorbed, and absorbing is not hitting. The cure was not more pinning but a SHORTER window: fire from 90 px so the shot lands within five frames, inside which the entity's state machine cannot turn over. It also failed ALONE and passed in the suite, which is the usual order-dependence inverted and a sign the cause is timing rather than leaked state. When a test must race a live entity, shorten the race rather than freezing more of the world.", "refs": ["src/legacy/game.test.ts", "green-alone-red-in-suite"], "session": "feat/ultimates"}
